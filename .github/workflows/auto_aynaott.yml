name: 📺 Auto Update AynaOTT M3U

on:
  workflow_dispatch:  # 👉 ম্যানুয়ালি রান করা যাবে
  schedule:
    - cron: "*/15 * * * *"  # ⏰ প্রতি ১৫ মিনিটে স্বয়ংক্রিয়ভাবে রান হবে (UTC টাইম)

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # ✅ GitHub Actions কে push করার অনুমতি দেয়

    steps:
    - name: 🔹 Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: 🔹 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.x"

    - name: 🔹 Install dependencies
      run: |
        pip install requests

    - name: 🔹 Run AynaOTT Playlist Generator
      run: |
        python Script/aynaott_auto.py

    - name: 🔹 Pull latest changes to avoid push conflict
      run: |
        git pull origin main --rebase || true

    - name: 🔹 Commit and Push Updated M3U
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add AynaOTT.m3u
        git commit -m "✅ Auto Updated AynaOTT.m3u on $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
        git push origin HEAD:${{ github.ref }}
