name: 🎬 Auto Combine M3U Playlist

on:
  workflow_dispatch:          # ✅ ম্যানুয়ালি ও orchestrator.yml থেকে চালানো যাবে

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: ▶️ Run Combine Script
        run: python Script/combine_m3u.py   # ✅ সঠিক path দেওয়া আছে

      - name: 💾 Commit & Push Combined Playlist
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          if [ -f Combined_Live_TV.m3u ]; then
            git add Combined_Live_TV.m3u
            git commit -m "🔄 Auto update Combined_Live_TV.m3u" || echo "No changes to commit"
            git push || true
          else
            echo "❌ Combined_Live_TV.m3u not found!"
            exit 1
          fi
