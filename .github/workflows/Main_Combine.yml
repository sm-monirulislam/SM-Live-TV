name: 🔁 Auto Combine IPTV & Check Links

on:
  schedule:
    - cron: "*/30 * * * *"   # ⏱️ প্রতি ৩০ মিনিটে রান হবে
  workflow_dispatch:          # 🧑‍💻 ম্যানুয়ালি চালানো যাবে

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 📚 Install Dependencies
        run: |
          pip install requests

      - name: ⚙️ Run Combine Script from /Script Folder
        run: |
          cd Script
          python combine_check.py
        working-directory: ./  # root directory of repo

      - name: 💾 Commit Updated Files
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add Script/Combined_Live_TV.m3u Script/offline.m3u Script/invalid_links.log || true
          git commit -m "🔁 Auto update IPTV files [$(date)]" || echo "No changes to commit"
          git push
