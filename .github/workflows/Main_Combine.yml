name: 📺 Auto Combine Live TV M3U

on:
  workflow_dispatch:  # 👉 ম্যানুয়ালি চালানো যাবে
  schedule:
    - cron: "*/30 * * * *"  # ⏰ প্রতি ৩০ মিনিটে রান হবে (UTC টাইম অনুযায়ী)

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🔹 Checkout repository
      uses: actions/checkout@v4

    - name: 🐍 Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: 📦 Install dependencies (if any)
      run: |
        python -m pip install --upgrade pip

    - name: 🧠 Run Combine Playlist Script
      run: |
        python Script/combine_playlist.py

    - name: 🕒 Commit and push updated M3U
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "actions@github.com"
        git add Combined_Live_TV.m3u
        git commit -m "✅ Auto updated Combined_Live_TV.m3u at $(date '+%Y-%m-%d %H:%M:%S')"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
