name: 🔁 RoarZone Auto Update Playlist

on:
  workflow_call:        # ✅ main controller থেকে কল করার জন্য আবশ্যক
  workflow_dispatch:    # ▶️ ম্যানুয়ালি রান করা যাবে

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 📦 Install Required Packages
        run: |
          pip install requests

      - name: 🔄 Run Auto Playlist Script
        run: |
          python3 Script/Roar-Zone.py

      - name: 💾 Commit & Push Changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          git add RoarZone.m3u
          if ! git diff --cached --quiet; then
            git commit -m "🔄 Auto-update RoarZone.m3u - $(date)"
            git push
          else
            echo "⚠️ No changes to commit."
          fi
