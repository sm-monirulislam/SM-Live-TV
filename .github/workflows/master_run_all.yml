name: 🚀 Run All Playlist Updates (Main Controller)

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"   # প্রতি ৩০ মিনিটে অটো রান (UTC)

jobs:

  aynaott:
    uses: ./.github/workflows/auto_aynaott.yml
    continue-on-error: true   # ❗ Fail হলেও workflow থামবে না

  crichd:
    needs: aynaott
    uses: ./.github/workflows/CricHD_update_playlist.yml
    continue-on-error: true

  fancode:
    needs: crichd
    uses: ./.github/workflows/Fancode_update.yml
    continue-on-error: true

  roarzone:
    needs: fancode
    uses: ./.github/workflows/Roar-Zone.yml
    continue-on-error: true
