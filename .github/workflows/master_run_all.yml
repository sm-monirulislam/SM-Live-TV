name: Run All Playlist Updates (Main Controller)

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

permissions:
  contents: write
  id-token: write

jobs:
  fancode:
    uses: ./.github/workflows/Fancode_update.yml
    secrets: inherit

  aynaott:
    needs: fancode
    if: always()
    uses: ./.github/workflows/auto_aynaott.yml
    secrets: inherit

  tapmad:
    needs: aynaott
    if: always()
    uses: ./.github/workflows/Tapmad_bd.yml
    secrets: inherit

  main_combined:
    needs: tapmad
    if: always()
    uses: ./.github/workflows/main_combined.yml
    secrets: inherit
